environment: staging
app_version: "1.0.0"
deployment_sha: ""  # set via DEPLOYMENT_SHA env var on deploy
crew_versions:
  researcher: "1.0.0"
  fitness_training: "1.0.0"
model_defaults:
  default: gpt-4o-mini
